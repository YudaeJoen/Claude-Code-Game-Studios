---
description: "Generate design or architecture documents from existing implementation. Works backwards from code/prototypes to create missing planning docs."
---

<!-- GENERATED from .claude/skills/reverse-document/SKILL.md — do not edit. Run tools/opencode/generate-adapter.py -->

Load the CCGS skill `reverse-document` by calling the skill tool: skill({ name: "reverse-document" })

Then follow that skill's instructions exactly, start to finish. It is authoritative over any default approach you would otherwise take.

Expected arguments: `<type> <path> (e.g., 'design src/gameplay/combat' or 'architecture src/core')`

Arguments supplied by the user: $ARGUMENTS