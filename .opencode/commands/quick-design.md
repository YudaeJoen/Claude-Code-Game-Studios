---
description: "Lightweight design spec for small changes — tuning adjustments, minor mechanics, balance tweaks. Skips full GDD authoring when a system GDD already exists or the change is too small to warrant one. Produces a Quick Design Spec that embeds directly into story files."
---

<!-- GENERATED from .claude/skills/quick-design/SKILL.md — do not edit. Run tools/opencode/generate-adapter.py -->

Load the CCGS skill `quick-design` by calling the skill tool: skill({ name: "quick-design" })

Then follow that skill's instructions exactly, start to finish. It is authoritative over any default approach you would otherwise take.

Expected arguments: `[brief description of the change]`

Arguments supplied by the user: $ARGUMENTS