---
description: "Emergency fix workflow that bypasses normal sprint processes with a full audit trail. Creates hotfix branch, tracks approvals, and ensures the fix is backported correctly."
---

<!-- GENERATED from .claude/skills/hotfix/SKILL.md — do not edit. Run tools/opencode/generate-adapter.py -->

Load the CCGS skill `hotfix` by calling the skill tool: skill({ name: "hotfix" })

Then follow that skill's instructions exactly, start to finish. It is authoritative over any default approach you would otherwise take.

Expected arguments: `[bug-id or description]`

Arguments supplied by the user: $ARGUMENTS