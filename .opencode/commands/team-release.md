---
description: "Orchestrate the release team: coordinates release-manager, qa-lead, devops-engineer, and producer to execute a release from candidate to deployment."
---

<!-- GENERATED from .claude/skills/team-release/SKILL.md — do not edit. Run tools/opencode/generate-adapter.py -->

Load the CCGS skill `team-release` by calling the skill tool: skill({ name: "team-release" })

Then follow that skill's instructions exactly, start to finish. It is authoritative over any default approach you would otherwise take.

Expected arguments: `[version number or 'next']`

Arguments supplied by the user: $ARGUMENTS