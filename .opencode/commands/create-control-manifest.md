---
description: "After architecture is complete, produces a flat actionable rules sheet for programmers — what you must do, what you must never do, per system and per layer. Extracted from all Accepted ADRs, technical preferences, and engine reference docs. More immediately actionable than ADRs (which explain why)."
agent: technical-director
---

<!-- GENERATED from .claude/skills/create-control-manifest/SKILL.md — do not edit. Run tools/opencode/generate-adapter.py -->

Load the CCGS skill `create-control-manifest` by calling the skill tool: skill({ name: "create-control-manifest" })

Then follow that skill's instructions exactly, start to finish. It is authoritative over any default approach you would otherwise take.

Expected arguments: `[update — regenerate from current ADRs]`

Arguments supplied by the user: $ARGUMENTS